
import numpy as np

import matplotlib.colors as mcolors

print(np.array(mcolors.to_rgb('blue')).dot((255)))
print(np.array(mcolors.to_rgb('cyan')).dot((255)))
print(np.array(mcolors.to_rgb('pink')).dot((255)))
print(np.array(mcolors.to_rgb('red')).dot(255))
